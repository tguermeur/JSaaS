/**
 * Fonction helper pour les logs de debug qui envoie des données à un serveur de tracking local
 * DÉSACTIVÉE pour éviter les erreurs de connexion dans la console
 */
export function debugLog(
  location: string,
  message: string,
  data: any,
  hypothesisId = 'default'
): void {
  // Fonction complètement désactivée pour éviter les erreurs de connexion
  return;
}
